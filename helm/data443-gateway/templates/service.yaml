apiVersion: v1
kind: Service
metadata:
  name: {{ include "data443-gateway.fullname" . }}
  labels:
    {{- include "data443-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "data443-gateway.selectorLabels" . | nindent 4 }}
